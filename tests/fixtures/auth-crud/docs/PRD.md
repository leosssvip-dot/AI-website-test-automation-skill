# Authenticated Project Manager PRD

## Requirements

- Users must log in before opening `/projects`.
- Users can list projects, create a project, edit a project name, and archive a project.
- Disabled users must not access project management.
- Project names are required and must be unique per account.
